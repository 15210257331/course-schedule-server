<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenxiaofei.coursescheduleserver.attachment.mapper.AttachmentMapper">

    <select id="listByTemplate" resultType="com.chenxiaofei.coursescheduleserver.attachment.entity.Attachment">
        SELECT * FROM attachment
        WHERE user_id = #{userId} AND template_id = #{templateId}
        ORDER BY id DESC
    </select>

    <select id="listByUser" resultType="com.chenxiaofei.coursescheduleserver.attachment.entity.Attachment">
        SELECT * FROM attachment
        WHERE user_id = #{userId}
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="com.chenxiaofei.coursescheduleserver.attachment.entity.Attachment">
        SELECT * FROM attachment
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO attachment(user_id, template_id, file_name, file_path, file_size, mime_type)
        VALUES(#{userId}, #{templateId}, #{fileName}, #{filePath}, #{fileSize}, #{mimeType})
    </insert>

    <delete id="delete">
        DELETE FROM attachment WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <delete id="deleteByTemplate">
        DELETE FROM attachment WHERE user_id = #{userId} AND template_id = #{templateId}
    </delete>

</mapper>
