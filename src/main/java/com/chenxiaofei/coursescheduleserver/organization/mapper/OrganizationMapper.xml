<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenxiaofei.coursescheduleserver.organization.mapper.OrganizationMapper">

    <select id="listByUser" resultType="com.chenxiaofei.coursescheduleserver.organization.entity.Organization">
        SELECT * FROM organization WHERE user_id = #{userId}
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        ORDER BY id DESC
    </select>

    <sql id="orgWhere">
        WHERE user_id = #{userId}
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
    </sql>

    <select id="countByUser" resultType="long">
        SELECT COUNT(*) FROM organization
        <include refid="orgWhere"/>
    </select>

    <select id="pageByUser" resultType="com.chenxiaofei.coursescheduleserver.organization.entity.Organization">
        SELECT * FROM organization
        <include refid="orgWhere"/>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findById" resultType="com.chenxiaofei.coursescheduleserver.organization.entity.Organization">
        SELECT * FROM organization WHERE id = #{id} AND user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO organization(user_id, name, contact_name, contact_phone, address, color, remark)
        VALUES(#{userId}, #{name}, #{contactName}, #{contactPhone}, #{address}, #{color}, #{remark})
    </insert>

    <update id="update">
        UPDATE organization SET name = #{name}, contact_name = #{contactName},
        contact_phone = #{contactPhone}, address = #{address},
        color = #{color}, remark = #{remark} WHERE id = #{id} AND user_id = #{userId}
    </update>

    <delete id="delete">
        DELETE FROM organization WHERE id = #{id} AND user_id = #{userId}
    </delete>

</mapper>
