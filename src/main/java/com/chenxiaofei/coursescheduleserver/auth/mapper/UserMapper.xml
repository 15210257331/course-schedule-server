<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenxiaofei.coursescheduleserver.auth.mapper.UserMapper">

    <select id="findById" resultType="com.chenxiaofei.coursescheduleserver.auth.entity.User">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

    <select id="findByUsername" resultType="com.chenxiaofei.coursescheduleserver.auth.entity.User">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <select id="findByEmail" resultType="com.chenxiaofei.coursescheduleserver.auth.entity.User">
        SELECT * FROM `user` WHERE email = #{email} LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user`(username, password, nickname, avatar, email, phone, subjects, role)
        VALUES(#{username}, #{password}, #{nickname}, #{avatar}, #{email}, #{phone}, #{subjects}, #{role})
    </insert>

    <update id="updateProfile">
        UPDATE `user` SET nickname = #{nickname}, avatar = #{avatar}, email = #{email},
        phone = #{phone}, subjects = #{subjects} WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE `user` SET password = #{password} WHERE id = #{id}
    </update>

    <update id="updateLastLoginAt">
        UPDATE `user` SET last_login_at = NOW() WHERE id = #{id}
    </update>

    <!-- ========== 管理端 ========== -->

    <sql id="teacherWhere">
        WHERE role = 'TEACHER'
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (username LIKE CONCAT('%', #{keyword}, '%')
                 OR nickname LIKE CONCAT('%', #{keyword}, '%')
                 OR email LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </sql>

    <select id="countTeachers" resultType="long">
        SELECT COUNT(*) FROM `user`
        <include refid="teacherWhere"/>
    </select>

    <select id="pageTeacherItems" resultType="com.chenxiaofei.coursescheduleserver.admin.dto.TeacherListItem">
        SELECT u.id, u.username, u.nickname, u.avatar, u.email, u.phone, u.subjects,
               u.status, u.disabled_reason AS disabledReason, u.disabled_at AS disabledAt,
               u.last_login_at AS lastLoginAt, u.created_at AS createdAt,
               (SELECT COUNT(*) FROM course c WHERE c.user_id = u.id) AS courseCount,
               (SELECT COUNT(DISTINCT c2.student_name) FROM course c2
                 WHERE c2.user_id = u.id AND c2.student_name IS NOT NULL AND c2.student_name != '') AS studentCount,
               (SELECT COUNT(*) FROM organization o WHERE o.user_id = u.id) AS organizationCount
        FROM `user` u
        <include refid="teacherWhere"/>
        ORDER BY u.id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <update id="updateStatus">
        UPDATE `user`
        SET status = #{status},
            disabled_reason = #{disabledReason},
            disabled_at = CASE WHEN #{status} = 'disabled' THEN NOW() ELSE NULL END
        WHERE id = #{id}
    </update>

    <select id="listTeachers" resultType="com.chenxiaofei.coursescheduleserver.auth.entity.User">
        SELECT id, username, nickname, avatar, email FROM `user`
        WHERE role = 'TEACHER' AND status = 'active'
        <if test="keyword != null and keyword != ''">
            AND (username LIKE CONCAT('%', #{keyword}, '%') OR nickname LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY id DESC
        LIMIT 200
    </select>

</mapper>
